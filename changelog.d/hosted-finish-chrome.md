### fix(planner): persist across Cast; show Lip-sync, Upscale, Blender

Cast hops no longer dump the planner behind a restore banner.
Render has first-class Lip-sync (on), Upscale (on), and Color
grade (off) so hosted users can name Blender without Operator
tools. Not deployed in this change.
