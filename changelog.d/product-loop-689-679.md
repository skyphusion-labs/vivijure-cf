MusicGen player loads the bed as a blob so duration is real (#689).
Motion cards are filmmaker copy, not Unified Billing (#679).
AV doors stamp has_audio. Voice lock + look lock prepend to every motion prompt so Flux/Seedance keep the same speaker.
